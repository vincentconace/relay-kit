# Implementation Log

> Produced by the `implementer` orchestrator (and the sub-agents it dispatches) on `/implement`. Consumed by the `reviewer`.

- Linked tasks: `.relay/features/<active>/tasks.md`
- Date: <YYYY-MM-DD>
- Task IDs executed: <T-001, T-002, ...>

---

## T-001 — <title from tasks.md>

- Executed by: <`self` | `frontend-implementer` | `backend-implementer` | …>
- Skill used: <name from `memory/skills.md` or `none — fallback applied`>
- What was done: <2–5 bullets, concrete.>
- Files changed:
  - `<path>` — <delta summary>.
- Deviations from plan: <`none` or describe + justify>.
- Surprises: <unexpected blockers, behavior, env quirks>.
- Sources consulted: <`[web:domain](url)` entries; or `none`>.
- Status: <`done` | `partial` | `blocked` (with reason)>.

## T-002 — <title>

- Executed by: …
- …

---

## Aggregate notes for the reviewer

- New skill candidates discovered: <name + when to use, or `none`>.
- New convention candidates: <description, or `none`>.
- Recommend re-running `/onboard --refresh`: <`yes` + reason | `no`>.
