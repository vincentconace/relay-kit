# Project Snapshot

> Canonical snapshot of this project. Produced by `/onboard`. Read by every relay-kit agent at the start of its turn.
> Re-run `/onboard --refresh` after structural changes (folder moves, framework swaps, new subsystems).

- Last updated: <YYYY-MM-DD>
- Generated by: relay-kit onboarder
- Greenfield: <yes | no>

## Stack

- Languages: <e.g. TypeScript 5.4, Python 3.12>
- Frameworks: <e.g. Next.js 14 App Router, FastAPI 0.110>
- Build tools: <e.g. pnpm, uv, turbo>
- Runtime: <e.g. Node 20, Python 3.12 venv>

## Frontend (if any)

- Framework: <e.g. Next.js 14 App Router, Vite + React 18, none>
- Styling: <e.g. Tailwind 3.4 with `@/styles/globals.css`>
- State: <e.g. Zustand for UI, TanStack Query for server state>
- Routing: <e.g. file-based under `app/`>

## Backend (if any)

- Framework: <e.g. FastAPI, Express 4, none>
- Persistence: <e.g. PostgreSQL via Prisma>
- Auth: <e.g. NextAuth with Google provider>
- API style: <e.g. REST under `/api/v1`, tRPC, GraphQL>

## Folder map

```
<project>/
├── <folder>/   # <semantic description>
└── ...
```

## Dev / Build / Test commands

- Install: `<command>`
- Dev: `<command>`
- Build: `<command>`
- Test: `<command>`
- Lint: `<command>`
- Format: `<command>`

## Entry points

- App entry: `<path>`
- Server entry: `<path>`
- Worker / cron entry: `<path>` (if any)

## Notable dependencies

- `<dep@version>` — why it matters in this codebase.

## Gotchas

- <Surprise that would bite a new contributor — env var, hidden script, broken default, etc.>
