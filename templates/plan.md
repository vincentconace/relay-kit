# Plan

> Produced by the `planner` agent on `/plan`. Consumed by the `task-maker`.

- Linked analysis: `.relay/features/<active>/analysis.md`
- Date: <YYYY-MM-DD>

## High-level approach

<Prose. 1 paragraph. The shape of the solution, not the steps.>

## Architecture decisions

- <Bullet. Each decision must cite either an existing `[memory:decisions#D-XXX]` it respects, or note "NEW — needs ADR" so the reviewer captures it.>

## Files to touch

- `<absolute project-relative path>` — <what changes and why>.
- `<absolute project-relative path>` — <what changes and why>.

## Deviations from existing conventions

- <Bullet. If none, write "Ninguna — el plan respeta `memory/conventions.md` íntegramente." Each deviation must justify itself.>

## Risks

- <Bullet — what could break, mitigation.>

## Rollback strategy

<How to undo this work if it ships and fails. Concrete: revert commit, feature flag, migration down step.>

## Sources

- <`[web:domain](url)` entries if WebSearch was used. Each must also be queued for `memory/references.md`.>
